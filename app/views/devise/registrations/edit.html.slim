.text-center
  = simple_form_for resource, as: resource_name, url: registration_path(resource_name),
      html: { method: :put, class: "edit-account" } do |f|
      h1.h3.mb-3.font-weight-normal Edit Account
      = f.input :full_name, label: false, required: true, autofocus: true, input_html: { class: "form-control" }
      = f.input :email, required: true, label: false, input_html: { class: "form-control" }
      = f.input :password, autocomplete: "off", label: false, required: false, input_html: { class: "form-control" }
      = f.button :submit, "Update", class: "btn btn-lg btn-primary btn-block"
  h6
    b Cancel my account
  p
    | Unhappy?
    '
    = link_to "Cancel my account.",
        registration_path(resource_name),
        data: { confirm: "Are you sure?" },
        method: :delete
